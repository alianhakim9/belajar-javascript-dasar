// melakukan iterasi terhadap isi value

const names = ["Alian", "Hakim"];

for (const name of names) {
  console.info(name);
}

const fullName = "Alian Hakim";
for (const character of fullName) {
  console.info(character);
}

const person = {
  firstName: "Alian",
  lastName: "Hakim",
};
